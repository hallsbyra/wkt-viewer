{
  "name": "wkt-viewer",
  "displayName": "WKT Viewer",
  "description": "A VS Code extension to view WKT geometries dynamically based on text selection.",
  "version": "0.0.1",
  "publisher": "your-publisher-name",
  "engines": {
    "vscode": "^1.99.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./extension/dist/extension.js",
  "workspaces": [
    "extension",
    "webview"
  ],
  "contributes": {
    "commands": [
      {
        "command": "wkt-viewer.helloWorld",
        "title": "Hello World"
      },
      {
        "command": "wktViewer.start",
        "title": "Start WKT Viewer"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run -w webview build && npm run -w extension compile",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@typescript-eslint/eslint-plugin": "^8.28.0",
    "@typescript-eslint/parser": "^8.28.0",
    "@vitejs/plugin-react": "^4.4.1",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.3.2",
    "eslint": "^9.23.0",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "ol": "^10.5.0"
  }
}
