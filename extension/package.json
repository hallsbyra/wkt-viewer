{
  "name": "extension",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc -p ./",
    "test": "npm --prefix .. run test:extension"
  },
  "devDependencies": {
    "@types/node": "^24.8.1",
    "@types/vscode": "^1.105.0"
  }
}
