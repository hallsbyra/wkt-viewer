{
  "name": "extension",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc -b ../shared && tsc -p ./",
    "test": "npm --prefix .. run test:extension"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/vscode": "^1.118.0",
    "@wkt-viewer/shared": "0.0.0"
  }
}
