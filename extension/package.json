{
  "name": "extension",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc -p ./",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "@types/vscode": "^1.104.0"
  }
}
